import React from 'react'

import { HomePage } from '../Pages/HomePage';


//Probably this is where the router will be
const Content = (props) => {
    return(
        <main>
            <HomePage />
        </main>
    );
}

export { Content }